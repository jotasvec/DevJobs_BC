Run the full verification cycle for the backend:

1. Build TypeScript: `pnpm --filter @devjobs/api build`
2. Run tests: `pnpm --filter @devjobs/api test`

Report pass/fail for each step. If tests fail, show the failing test names and error messages.
