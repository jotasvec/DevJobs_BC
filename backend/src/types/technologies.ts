export interface Technology{
    id: string
    name: string
    category: string
    created_at: string
}

export interface TechnologyCategory{
    id: string
    name: string
    description: string
}